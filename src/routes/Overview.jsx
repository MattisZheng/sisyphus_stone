import { useContext } from "react";

const Overview = () => {
  // const { tasks, setTasks } = useContext(DataContext);
  
  return <div>
    tasks
    {/* {tasks} */}
  </div>;
};

export default Overview;
