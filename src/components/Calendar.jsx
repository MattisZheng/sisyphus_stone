import { Calendar } from "antd";

const Calenda = () => {
  // get history
  // get weather

  // zoom
  // 1 week
  // 1 month
  return (
    <div>
      <Calendar />
    </div>
  );
};

export default Calendar;
