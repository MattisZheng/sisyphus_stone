import React from "react";

const TaskBox = ({ task }) => {
  // add tasks
  // delete tasks
  // update tasks

  return (
    <div>
      TaskBox
      {task}
    </div>
  );
};

export default TaskBox;
